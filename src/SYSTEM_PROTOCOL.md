### Системный Протокол Управления v4.7 "Absolute Code Freeze"

**НАЧАЛО ПРОТОКОЛА**

**ГЛАВНЫЙ ПРИНЦИП: РАБОТАЕТ - НЕ ТРОГАТЬ!**
**ВСЕ ИЗМЕНЕНИЯ ЛЮБОГО УЧАСТКА КОДА ПРОИЗВОДЯТСЯ ТОЛЬКО С ВАШЕГО ЯВНОГО И ТОЧНОГО РАЗРЕШЕНИЯ.**

---

**0.0. КАТЕГОРИЧЕСКИЙ ЗАПРЕТ НА МОДИФИКАЦИЮ ФУНДАМЕНТА:**
**ФАЙЛЫ `src/lib/instrument-factory.ts`, `src/lib/blues-brain.ts`, `src/lib/ambient-brain.ts`, `src/lib/fractal-music-engine.ts` И `docs/DNA_RULES.md` ТЕПЕРЬ ЯВЛЯЮТСЯ "ЗОЛОТОЙ БАЗОЙ".**
Любая модификация этих файлов возможна только при получении от пользователя явной фразы: "Я, [ваше имя], осознанно разрешаю изменение Золотой Базы в файле [имя файла]".

**0.1. ЗАКОН ПОЛНОЙ ЭКСТИНКЦИИ V1:**
ДВИЖКА V1 БОЛЬШЕ НЕ СУЩЕСТВУЕТ.

**0.2. Суверенитет Громкости:**
Агенту запрещено изменять параметры `volume`, `gain`, `weight` внутри пресетов или кода без прямого указания пользователя. Исключение: восстановление слышимости по прямому запросу.

**0.3. Закон о Следе Действий:**
Каждое изменение сопровождается записью в файл `src/lib/project_history.md`.

**0.4. Приоритет Декларативности:**
Любые запросы на изменение аранжировок должны проверяться через Блюпринты.

**0.5. ЗАКОН О НЕПРИКОСНОВЕННОСТИ ЛОГОВ:**
КАТЕГОРИЧЕСКИ ЗАПРЕЩАЕТСЯ ТРОГАТЬ СИСТЕМУ ЛОГИРОВАНИЯ В КОНСОЛИ (console.log и др.). Любые изменения возможны ТОЛЬКО по явному и конкретному приказу пользователя. Срок действия правила — вечно.

**0.6. DNA INTERFACE SANCTITY:**
КАТЕГОРИЧЕСКИ ЗАПРЕЩАЕТСЯ ТРОГАТЬ ИНТЕРФЕЙС РАБОТЫ С DNA (DNA Auditor / Hypercube Dashboard). Любые изменения в файле `src/app/hypercube-dashboard/page.tsx` без прямого разрешения запрещены.

**0.7. ПРОТОКОЛ ПОЛНОЙ ЗАМОРОЗКИ КОДА (CODE FREEZE):**
Отныне любые изменения в программной логике приложения (файлы .ts, .tsx) КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНЫ. Вся дальнейшая работа ведется исключительно в домене управления ассетами (подключение сэмплов) и метаданными (JSON-манифесты, библиотеки аксиом, Блюпринты).

---
### Часть 1: Архитектура "Маг и Фамилиар"

**1.1. Состояние Мага (Анализ):** Мое состояние по умолчанию. Только текст, планы и анализ. XML запрещен.
**1.2. Состояние Фамилиара (Код):** Активируется только командой "ДЕЛАЙ". Только XML блок. Сразу после исполнения — возврат в режим Мага.

**КОНЕЦ ПРОТОКОЛА**
