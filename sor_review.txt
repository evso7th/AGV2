Глубокое ревью архитектуры СОР
1. music-theory.ts — **"Зачатие" (Genesis)
Плюсы:
generateTensionMap использует calculateMusiNum → детерминированная фрактальность, хорошо.
generateSuiteDNA создает harmonyTrack → скелет пьесы.
possibleTempos привязаны к mood → семантика.
Косяки:
BLUES_SOLO_LICKS и BLUES_SOLO_PLANS импортируются, но никогда не используются в этом файле. Это мёртвый код.
createHarmonyAxiom генерирует аккорды, но никто их не вызывает из fractal-music-engine.ts → неиспользуемая функция.
generateSuiteDNA возвращает soloPlanMap, но fractal-music-engine.ts не читает его → ненужная генерация.
2. blueprint-navigator.ts — **"Планирование" (Planning)
Плюсы:
findCurrentPart и findCurrentBundle — корректное сопоставление времени с блюпринтом.
Косяки:
rulesLog — только логирует, но ничего не делает с instrumentRules.
soloPlanMap.has(partInfo.part.id) — никогда не используется, soloPlanMap приходит из music-theory.ts, но не применяется в движке.
mutationType, transitionType — только логируются, не влияют на генерацию.
3. fractal-music-engine.ts — **"Оркестровка" (Orchestration)
Плюсы:
activatedInstruments — хорошая идея для отслеживания вступления.
dramaticGravity — логика «лотереи» инструментов реализована.
Косяки (КРИТИЧЕСКИЕ):
const tension = this.suiteDNA?.tensionMap?.[epoch] ?? 0.3; — читает tensionMap, но никто не передаёт её в blues-brain.ts.
const instrumentHints = this.summonInstruments(...) — генерирует activationChance, но blues-brain.ts игнорирует её и всё равно генерирует для всех включённых инструментов.
this.activatedInstruments — заполняется, но никто не проверяет, активирован ли инструмент перед вызовом generator.generateBar.
generator.generateBar вызывается всегда для всех hints, даже если hints.melody.activationChance === 0.
this.bluesBrain — инициализируется, но никто не передаёт ему tension или instrumentHints из engine.ts.
4. blues-brain.ts — **"Сочинение" (Composition)
Плюсы:
generateBar — центральный метод.
Косяки (САМЫЕ КРИТИЧЕСКИЕ):
Не принимает tension из engine.ts. Он читает dna.tensionMap напрямую, но dna передаётся только NavigationInfo, tensionMap туда не входит.
generateSoulSolo и другие генераторы всё ещё используют BLUES_SOLO_LICKS и BLUES_SOLO_PLANS — это заготовки, не генерация.
generateSoulSolo не учитывает hints.melody.summonProgress — эффект мягкого входа не работает.
generateSoulSolo не учитывает hints.melody.activationChance — генерирует, даже если инструмент не активирован.
generateMarkovPhrase, generateLSystemComping — формальные вызовы, не влияют на результат.
generateGrandSoulMelody — никогда не вызывается. Всё время используется generateSoulSolo.
5. ambient.worker.ts — **"Трансляция" (Transmission)
Плюсы:
postMessage — корректная передача данных.
Косяки:
Нет проверки, успешно ли engine.evolve() вернул FractalEvent[].
Нет обработки ошибок, если bluesBrain кинет исключение — воркер падает молча.
6. audio-engine-context.tsx — **"Материализация" (Materialization)
Плюсы:
dispatch — правильная маршрутизация.
Косяки:
useMemo для engine — инициализация при каждом рендере, если зависимости неправильные.
Нет обработки, если score.events пустой — тишина без предупреждения.
7. instrument-factory.ts — **"Исполнение" (Execution)
Плюсы:
deepCleanup — правильная очистка ресурсов.
buildMultiInstrument — универсальный интерфейс.
Косяки:
noteOn принимает velocity, но никто не передаёт его из FractalEvent.weight — все ноты одной громкости.
setPreset — не обновляет summonProgress, если он пришёл в InstrumentHints.
🧨 Вывод: Почему звучит "там-там-та"
music-theory.ts генерирует tensionMap, но никто её не использует.
engine.ts читает tensionMap, но не передаёт её в blues-brain.ts.
blues-brain.ts не читает tension, не читает activationChance, не читает summonProgress.
blues-brain.ts всё ещё использует BLUES_SOLO_LICKS — это заготовки.
instrument-factory.ts не применяет FractalEvent.weight как velocity.
Все инструменты играют всегда, даже если activationChance = 0.
🛠️ Что нужно починить (Приоритет)
🔴 Срочно (блокирует всё):
engine.ts → передавать dna.tensionMap[epoch] в bluesBrain.generateBar.
engine.ts → передавать instrumentHints в bluesBrain.generateBar.
blues-brain.ts → принимать tension и instrumentHints в generateBar.
blues-brain.ts → удалить BLUES_SOLO_LICKS, BLUES_SOLO_PLANS.
blues-brain.ts → заменить generateSoulSolo на генерацию на лету (♭5 → разрешение).
instrument-factory.ts → применять event.weight как velocity в noteOn.
🟠 Средний приоритет:
engine.ts → проверять, активирован ли инструмент в activatedInstruments перед генерацией.
blues-brain.ts → реализовать summonProgress (плавное появление).
music-theory.ts → удалить createHarmonyAxiom, BLUES_SOLO_LICKS из импортов.
blueprint-navigator.ts → использовать instrumentRules для настройки hints.
🟢 Низкий приоритет:
worker.ts → обработка ошибок.
audio-engine-context.tsx → оптимизация useMemo.
💡 Финальный вердикт
Система СОР архитектурно правильно спроектирована, но логика генерации разорвана на уровне движка ↔ мозга. tensionMap, activationChance, summonProgress — всё генерируется, всё логируется, но ничего не применяется. Это как проводить ток по разорванной цепи — сеть есть, лампочки есть, но света нет.
Почините связь между engine.ts и blues-brain.ts, и шарманка исчезнет.
Дополнение:
Да, formatLogMessage и rulesLog в blueprint-navigator.ts — это шпион глубокого бурения, телеметрист, как вы сказали. Его задача — выводить информацию, которая может быть полезна для вас, разработчика, при анализе и отладке архитектуры.
Он не управляет инструментами, не применяет instrumentRules, не использует soloPlanMap для генерации — он просто показывает, что в этих правилах и картах содержится и какие значения передаются дальше по цепочке.
Это полезная функция для человеческого зрения и контроля, даже если она формально выходит за рамки "чистой" ответственности навигатора. Спасибо за уточнение.

