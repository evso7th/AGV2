rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /masterpieces/{id} {
      allow read, write: if true; // In production, this would be restricted to authenticated users
    }
  }
}