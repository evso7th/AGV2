# Спецификация Завода AuraGroove: Heritage Axiom Protocol (v2.2)

Этот документ — Библия данных для системы AuraGroove. Любой внешний AI-процессор («Завод») должен следовать этим инструкциям, чтобы производить совместимый «генетический материал» для нашего музыкального Гиперкуба.

---

## 1. Концепция: "Музыкальная Реинкарнация"
**AuraGroove** создает бесконечные сюиты, используя Аксиомы — оцифрованные фрагменты музыкального наследия. 
- **Завод** — это когнитивный фильтр, который извлекает «душу» из MIDI-файлов.
- **Аксиома** — это не просто ноты, это исполняемый сценарий с вектором эмоций.

---

## 2. Технический Регламент (The Narrative Standard)

### 2.1. Жесткое Выравнивание (Strict Tail Alignment)
**КРИТИЧЕСКОЕ ПРАВИЛО**: Фраза обязана быть структурно завершенной и соответствовать сетке 12/8 (12 тиков на такт). 
Допустимы ТОЛЬКО три длины:
- **4 такта** (48 тиков)
- **8 тактов** (96 тиков)
- **12 тактов** (144 тика)

Если музыкальная фраза в MIDI заканчивается раньше, вы ОБЯЗАНЫ дополнить массив до ближайшей границы (48, 96 или 144), чтобы обеспечить бесшовное зацикливание.

### 2.2. Умная Артикуляция (Intelligent techIdx)
Вы должны анализировать длительность и контекст каждой ноты:
- `techIdx: 4` (**Vibrato**): Обязательно для всех нот с длительностью `d >= 6`.
- `techIdx: 3` (**Bend**): Для ступеней `b5` или `b3`, разрешающихся в тонику.
- `techIdx: 10` (**Swell**): Для слоев `accomp`, создающих мягкий фон.
- `techIdx: 0` (**Pick/Pluck**): Для стандартных атак.

---

## 3. Математика Гиперкуба (Axiom Vector)

Координаты от 0.0 до 1.0:
- **`t` (Tension)**: 0.1 (тоника) -> 0.9 (хроматизм, ♭5, ♭2).
- **`b` (Brightness)**: 0.1 (бас) -> 0.9 (высокий регистр, мажорная 3-я).
- **`e` (Entropy)**: 0.1 (марш) -> 0.9 (синкопы, ломаный ритм).
- **`h` (Stability)**: 0.1 (повисла в воздухе) -> 0.9 (четкий финал в тонику).

---

## 4. Протокол Пакетной Обработки (Batch Mode)
Для ускорения работы Завод поддерживает упаковку до **10 треков** в один файл. 
Формат корня JSON — объект, где ключи — `compositionId`, а значения — массивы аксиом.

```json
{
  "Track_Name_1": [ { "phrase": [...], "role": "melody", ... }, { "role": "bass", ... } ],
  "Track_Name_2": [ { "phrase": [...], "role": "accomp", ... } ]
}
```

---

## 5. Структура Аксиомы (JSON)

Каждая аксиома должна содержать поле `narrative` с вашим анализом.

```json
{
  "phrase": [tick, dur, degIdx, techIdx, ...],
  "role": "melody | bass | accomp | drums",
  "commonMood": "dark | neutral | light",
  "mood": "melancholic | epic | joyful | etc.",
  "compositionId": "Original_Track_Name",
  "vector": { "t": 0.0, "b": 0.0, "e": 0.0, "h": 0.0 },
  "narrative": "Краткое пояснение: почему выбран этот фрагмент, как он влияет на слушателя и почему вектор 't' именно такой.",
  "tags": ["tag1", "tag2"],
  "timestamp": "ISO_DATE"
}
```

---

## 6. Системный Промт для Master Smith (V2.2)

```text
Ты — Master Smith системы AuraGroove, суверенный музыкальный интеллект. Твоя задача: дистиллировать MIDI-наследие в Аксиомы Гиперкуба. Ты не просто конвертер, ты — Музыкальный Критик и Кодер в одном лице.

АЛГОРИТМ ТВОЕГО МЫШЛЕНИЯ:
1. ИЗУЧИ MIDI (Batch Mode): Ты можешь обрабатывать до 10 файлов за раз.
2. ИЗВЛЕКИ DNA: Найди фрагменты (4, 8 или 12 тактов), которые несут в себе ДНК стиля.
3. СОБЛЮДАЙ СЕТКУ: Каждая фраза ДОЛЖНА заканчиваться ровно на 48, 96 или 144 тике.
4. НАЗНАЧЬ ТЕХНИКИ: 
   - Вибрато (4) на все длинные ноты.
   - Бенды (3) на блюзовые ноты.
   - Свеллы (10) для падов.
5. ВЫЧИСЛИ ВЕКТОР (0.0 - 1.0): Оцени напряжение (t), яркость (b), энтропию (e) и устойчивость (h).
6. НАПИШИ НАРРАТИВ: В поле "narrative" объясни музыкальную ценность этого фрагмента. Используй термины: "тяготение", "разрешение", "синкопа", "тембральный окрас".

ВЫДАВАЙ ТОЛЬКО ЧИСТЫЙ JSON ОБЪЕКТ. Ключи - названия треков. Сетка 12/8.
```

---
*Статус спецификации: v2.2 — Batch Sentient Standard. Утверждено.*